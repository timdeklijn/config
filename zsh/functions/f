# Move to '~/workspace` folder, list all folders in there and
# move into the chosen directory
f() {
  cd ~/workspace
  local dir
  dir=$(find ${1:-.} -path '*/\.*' -prune \
                  -o -type d -print 2> /dev/null | fzf +m) &&
  cd "$dir"
}

f
