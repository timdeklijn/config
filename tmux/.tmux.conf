# =============================================================================
#
# Tim de Klijn, 2021
# TMUX config
# - Leader: C-a
# - VI mode for copy/paste into pbcopy (system buffer)
# - Seamless VIM/TMUX pane switching
#
# =============================================================================

# reset leader key
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# change window split keys and open new pane in same path as the active pane
bind | split-window -h -c "#{pane_current_path}"
bind _ split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# Reload tmux config
bind r source-file ~/.tmux.conf

# Change how the mouse behaves
set -g mouse on

# Remove escape delay
set -s escape-time 0

# always renumber windows when updated
set-option -g renumber-windows on

# =============================================================================
# VI like copying
# =============================================================================

# VI mode for copying (prefix "[")
setw -g mode-keys vi
set-window-option -g mode-keys vi

bind-key -T copy-mode-vi 'v' send -X begin-selection
# Send to system clipboard (works for MacOS Mojave)
bind-key -T copy-mode-vi 'y' send -X copy-pipe-and-cancel "pbcopy"

# =============================================================================
# Window resizing
# =============================================================================

bind J resize-pane -D 20
bind K resize-pane -U 20
bind L resize-pane -R 20
bind H resize-pane -L 20

# =============================================================================
# Integrate with VIM pane switching
# =============================================================================

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l

# =============================================================================
# Some visuals
# =============================================================================

# Set terminal colors
set -g default-terminal "screen-256color"
set -g default-terminal "tmux-256color"

set -ga terminal-overrides ",xterm-256color:Tc"
set -as terminal-overrides ',xterm*:sitm=\E[3m'

# Some general settings
set-option -g status-position top

# Set colors theme of the status bar. No background and a
# single color for the text.

# Dark Background:
set -g status-bg '#1E1E29'
set -g status-fg 'colour245'

# border colours sould be similar to the background color
# of the terminal colour theme. Find these colors in the
# "terminal -> profile -> colors -> Backround" theme.
set -g pane-border-style "bg=default fg=colour13, bold"
set -g pane-active-border-style "bg=default fg=colour13, bold"

set -g status-left-length 85
# set -g status-justify centre

# style left + right parts of status bar
set -g status-left-style "fg=colour1, bold"
set -g status-left ' #S '
set -g status-right-style "fg=colour1, bold"
set -g status-right " %Y-%m-%d %H:%M "

# style center pars of status bar
set-window-option -g window-status-current-style "fg=colour2, bold"
set-window-option -g window-status-separator '  '
setw -g window-status-format ' #I:#W '
setw -g window-status-current-format ' #I:#W '
set-window-option -g window-status-separator ''
